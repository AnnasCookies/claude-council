# Optional HTTP provider credentials. An unset key disables only that provider route.
OPENAI_API_KEY=
XAI_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=
# Anthropic uses a local Claude subscription CLI; it does not read an API key here.
